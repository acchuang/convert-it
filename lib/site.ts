// Canonical origin. Every absolute URL (metadata, sitemap, robots, JSON-LD)
// derives from here — the old convert-it.pages.dev values had drifted out of
// sync with the live domain and were pointing crawlers at a dead host.
export const SITE_URL = 'https://convert-it.oilygold.xyz';
